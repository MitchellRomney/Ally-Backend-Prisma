# Migration `20200412090317`

This migration has been generated by Mitchell Romney at 4/12/2020, 9:03:17 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Participant" ;

ALTER TABLE "public"."User" ADD COLUMN "test" text  NOT NULL ;

ALTER TABLE "public"."Participant" ADD FOREIGN KEY ("spell1Id")REFERENCES "public"."SummonerSpell"("key") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Participant" ADD FOREIGN KEY ("spell2Id")REFERENCES "public"."SummonerSpell"("key") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200410184016..20200412090317
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://mitchellromney:mitchellromney@localhost:5432/ally-gg"
 }
 model User {
   email    String     @default("") @unique
@@ -13,8 +13,9 @@
   password String     @default("")
   userId   Int        @default(autoincrement()) @id
   username String     @unique
   summoners Summoner[]
+  test     String
 }
 model Summoner {
   accountId     String        @default("")
@@ -330,24 +331,24 @@
   perk1Id                         Int?
   perk1Var1                       Int      @default(0)
   perk1Var2                       Int      @default(0)
   perk1Var3                       Int      @default(0)
-  perk2                           Rune?     @relation("ParticipantPerk2", fields: [perk2Id], references: [runeId])
+  perk2                           Rune?    @relation("ParticipantPerk2", fields: [perk2Id], references: [runeId])
   perk2Id                         Int?
   perk2Var1                       Int      @default(0)
   perk2Var2                       Int      @default(0)
   perk2Var3                       Int      @default(0)
-  perk3                           Rune?     @relation("ParticipantPerk3", fields: [perk3Id], references: [runeId])
+  perk3                           Rune?    @relation("ParticipantPerk3", fields: [perk3Id], references: [runeId])
   perk3Id                         Int?
   perk3Var1                       Int      @default(0)
   perk3Var2                       Int      @default(0)
   perk3Var3                       Int      @default(0)
-  perk4                           Rune?     @relation("ParticipantPerk4", fields: [perk4Id], references: [runeId])
+  perk4                           Rune?    @relation("ParticipantPerk4", fields: [perk4Id], references: [runeId])
   perk4Id                         Int?
   perk4Var1                       Int      @default(0)
   perk4Var2                       Int      @default(0)
   perk4Var3                       Int      @default(0)
-  perk5                           Rune?     @relation("ParticipantPerk5", fields: [perk5Id], references: [runeId])
+  perk5                           Rune?    @relation("ParticipantPerk5", fields: [perk5Id], references: [runeId])
   perk5Id                         Int?
   perk5Var1                       Int      @default(0)
   perk5Var2                       Int      @default(0)
   perk5Var3                       Int      @default(0)
```


