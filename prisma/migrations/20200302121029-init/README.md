# Migration `20200302121029-init`

This migration has been generated by Mitchell Romney at 3/2/2020, 12:10:29 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
    "email" text  NOT NULL DEFAULT '',
    "isAdmin" boolean  NOT NULL DEFAULT false,
    "password" text  NOT NULL DEFAULT '',
    "userId" SERIAL,
    "username" text   ,
    PRIMARY KEY ("userId")
) 

CREATE TABLE "public"."Summoner" (
    "accountId" text  NOT NULL DEFAULT '',
    "allyId" text  NOT NULL DEFAULT '',
    "allyName" text  NOT NULL DEFAULT '',
    "id" SERIAL,
    "profileIconId" integer  NOT NULL DEFAULT 0,
    "puuid" text  NOT NULL DEFAULT '',
    "server" text  NOT NULL DEFAULT '',
    "summonerId" text  NOT NULL DEFAULT '',
    "summonerLevel" integer  NOT NULL DEFAULT 0,
    "summonerName" text  NOT NULL DEFAULT '',
    "user" integer   ,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "User.username" ON "public"."User"("username")

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "Summoner.allyId" ON "public"."Summoner"("allyId")

CREATE UNIQUE INDEX "Summoner.allyName" ON "public"."Summoner"("allyName")

ALTER TABLE "public"."Summoner" ADD FOREIGN KEY ("user")REFERENCES "public"."User"("userId") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200302121029-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,30 @@
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://mitchellromney:mitchellromney@localhost:5432/ally-backend"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  userId Int @id @default(autoincrement())
+  username String? @unique
+  password String
+  email String @unique
+  summoners Summoner[]
+  isAdmin Boolean
+}
+
+model Summoner {
+  id Int @id @default(autoincrement())
+  allyId String @unique
+  allyName String @unique
+  summonerName String
+  summonerId String
+  puuid String
+  accountId String
+  profileIconId Int
+  summonerLevel Int
+  server String
+}
```


