# Migration `20200410184016`

This migration has been generated by Mitchell Romney at 4/10/2020, 6:40:16 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "public"."Participant_perk0Id"

DROP INDEX "public"."Participant_perk1Id"

DROP INDEX "public"."Participant_perk2Id"

DROP INDEX "public"."Participant_perk3Id"

DROP INDEX "public"."Participant_perk4Id"

DROP INDEX "public"."Participant_perk5Id"

DROP INDEX "public"."Participant_spell1Id"

DROP INDEX "public"."Participant_spell2Id"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200408150722-initial..20200410184016
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://mitchellromney:mitchellromney@localhost:5432/ally-gg"
 }
 model User {
   email    String     @default("") @unique
@@ -248,10 +248,10 @@
   summonerLevel   Int    @default(0)
   summonerSpellId String @default("")
   tooltip         String @default("")
   version         String @default("")
-  participantSpell1 Participant? @relation("ParticipantSpell1")
-  participantSpell2 Participant? @relation("ParticipantSpell2")
+  participantSpell1 Participant[] @relation("ParticipantSpell1")
+  participantSpell2 Participant[] @relation("ParticipantSpell2")
 }
 model Rune {
   icon      String @default("")
@@ -260,14 +260,14 @@
   name      String @default("")
   runeId    Int    @id
   shortDesc String @default("")
   version   String @default("")
-  participantPerk0 Participant? @relation("ParticipantPerk0")
-  participantPerk1 Participant? @relation("ParticipantPerk1")
-  participantPerk2 Participant? @relation("ParticipantPerk2")
-  participantPerk3 Participant? @relation("ParticipantPerk3")
-  participantPerk4 Participant? @relation("ParticipantPerk4")
-  participantPerk5 Participant? @relation("ParticipantPerk5")
+  participantPerk0 Participant[] @relation("ParticipantPerk0")
+  participantPerk1 Participant[] @relation("ParticipantPerk1")
+  participantPerk2 Participant[] @relation("ParticipantPerk2")
+  participantPerk3 Participant[] @relation("ParticipantPerk3")
+  participantPerk4 Participant[] @relation("ParticipantPerk4")
+  participantPerk5 Participant[] @relation("ParticipantPerk5")
 }
 model Participant {
   assists                         Int      @default(0)
```


